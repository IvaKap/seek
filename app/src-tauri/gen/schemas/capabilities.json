{"default":{"identifier":"default","description":"Seek's own commands plus the core defaults. Without a capability file Tauri v2 grants the webview nothing at all, and every invoke fails.","local":true,"windows":["main"],"permissions":["core:default","notification:default","dialog:allow-open"]}}