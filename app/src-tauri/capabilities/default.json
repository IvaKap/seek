{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Seek's own commands plus the core defaults. Without a capability file Tauri v2 grants the webview nothing at all, and every invoke fails.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart",
    "clipboard-manager:allow-write-text"
  ]
}
